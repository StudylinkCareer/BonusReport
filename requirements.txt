# Runtime dependencies for the StudyLink BonusReport engine.
# Verify these match your venv with: pip show <package>
# Install with: pip install -r requirements.txt

psycopg[binary]==3.3.4
python-dotenv==1.2.2
openpyxl==3.1.5
